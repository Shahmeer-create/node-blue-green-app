version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: <replace-with-green-task-definition-arn>
        LoadBalancerInfo:
          ContainerName: "my-node-app"
          ContainerPort: 3000
